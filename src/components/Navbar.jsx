import React from "react";

const Navbar = props => {
  return (
    <div id="nav-bar">
      <div id="nav-title">
        <h1>Algo Visualizer</h1>
      </div>
    </div>
  );
};

export default Navbar;
